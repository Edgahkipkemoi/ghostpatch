{
  "name": "ghostpatch",
  "version": "1.0.0",
  "description": "Frankenstein Hybrid Code Generator for Kiroween",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["kiroween", "frankenstein", "code-stitching"],
  "author": "GhostPatch Team",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
