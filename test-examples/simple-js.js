// Simple JavaScript example for testing
function greet(name) {
  return `Hello, ${name}!`;
}

function calculate(a, b) {
  return a + b;
}

const result = calculate(5, 10);
console.log(greet('GhostPatch'));
console.log('Result:', result);
